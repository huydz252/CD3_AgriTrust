<nav class="navbar navbar-expand-lg navbar-dark bg-dark shadow-sm mb-4">
    <div class="container">
        <a class="navbar-brand fw-bold" href="/">🍎 AgriTrust</a>
        <div class="ms-auto d-flex align-items-center">
            <% if (user) { %>
                <img src="<%= user.avatar_url %>" class="rounded-circle me-2" style="width: 30px; height: 30px;">
                <span class="text-white me-3"><%= user.display_name %></span>
                <a href="/auth/logout" class="btn btn-outline-light btn-sm">Đăng xuất</a>
            <% } else { %>
                <a href="/auth/google" class="btn btn-light btn-sm">Đăng nhập</a>
            <% } %>
        </div>
    </div>
</nav>